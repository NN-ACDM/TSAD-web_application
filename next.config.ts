import { NextConfig } from 'next'

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Add any other usual config here:
  // i18n, images, experimental features, etc.
}

export default nextConfig
