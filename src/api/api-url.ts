// import axios from 'axios';

// export const getTemporaryVideoUrl = async (fileName: string): Promise<string> => {
//   const res = await axios.post('/videos', { fileName });
//   return res.data.url;
// };
